type: string
description: >
  Статус импорта ExcelFile:
    * `new` - Новый
    * `running` - В процессе импорта
    * `completed` - Завершен
    * `error` - Ошибка
enum:
  - new
  - running
  - completed
  - error
example: new
